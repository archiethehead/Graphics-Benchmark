# Graphics-Benchmark
A C library for testing the performance of a program using a Graphics API, expressing this in the time it takes for a produced frame in ms, how many of those frames are made per second, draw calls per frame, and draw calls per second.
